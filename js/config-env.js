/**
 * ShakeMap Environment Overrides
 *
 * This file is replaced at Docker container startup based on SHAKEMAP_ENV.
 * For local development, it is empty (INGV defaults from config-base.js apply).
 *
 * To test a different environment locally:
 *   cp js/config-eu.js js/config-env.js
 */
